import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  AppLogo: {
    resizeMode: 'contain',
    height: 180,
    width: 180,
  },
  Tittle: {
    //textAlign: 'center',
    //justifyContent: 'center',
    color: 'white',
    fontSize: 30,
    fontWeight: 'bold',
  },
});
